#post_context
  %h1= @post.title
  %p= @post.context



#replies
  %h2= @post.replies.count
  Reply
  = render @post.replies


  %h3 Reply to thread 
  = render "replies/form"

	
	

= link_to "Edit", edit_post_path(@post)
|
= link_to "Delete", post_path(@post), method: :delete, data: { confirm: "Are you sure you want to do this?" }
|
= link_to "Back", posts_path  
